{
    "name": "xkbfile",
    "description": "xkbfile",
    "version": [ 1,1,1 ],
    "url": "https://www.x.org/archive/individual/lib/libxkbfile-1.1.1.tar.gz",
    "depends": [ "cross-gcc", "xorgproto", "libx11" ],
    "patches": [],
    "bootstrap-commands": [
        [ "cp", "${root_directory}/scripts/config.sub", "${repo_directory}/lib${name}-${version}/config.sub" ],
        [ "${repo_directory}/lib${name}-${version}/configure", "--host=${target_triplet}", "--prefix=${prefix}" ]
    ],
    "build-commands": [
        [ "make" ]
    ],
    "install-commands": [
        [ "make", "install" ],
        [ "make", "install", "prefix=${bin_package_prefix}/usr" ]
    ]
}
