{
    "name": "libfontenc",
    "description": "",
    "version": [ 1,1,8 ],
    "url": "https://www.x.org/archive/individual/lib/libfontenc-1.1.8.tar.gz",
    "depends": [ "cross-gcc", "xorgproto", "zlib" ],
    "patches": [],
    "bootstrap-commands": [
        [ "${root_directory}/scripts/do-autoreconf.sh", "${repo_directory}/${name}-${version}" ],
        [ "${repo_directory}/${name}-${version}/configure", "--host=${target_triplet}", "--prefix=/usr", "--disable-static", "--enable-shared" ]
    ],
    "build-commands": [
        [ "make" ]
    ],
    "install-commands": [
        [ "make", "install", "DESTDIR=${prefix}" ],
        [ "make", "install", "DESTDIR=${bin_package_prefix}" ]
    ]
}
