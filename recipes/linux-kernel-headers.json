{
    "name": "linux-kernel-headers",
    "description": "Linux kernel headers",
    "version": [ 6,12,38 ],
    "depends": [],
    "patches": [],
    "url": "https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.12.38.tar.xz",
    "bootstrap-commands": [],
    "build-commands": [],
    "install-commands": [
        [ "cd", "${repo_directory}/linux-6.12.38" ],
        [ "export", "target_triplet=${target_triplet}" ],
        [ "make", "headers_install", "ARCH=$${target_triplet%-*}", "INSTALL_HDR_PATH=${prefix}" ],
        [ "make", "headers_install", "ARCH=$${target_triplet%-*}", "INSTALL_HDR_PATH=${bin_package_prefix}/usr" ],
        [ "cd", "${bootstrap_directory}/linux-kernel-headers" ]
    ]
}
