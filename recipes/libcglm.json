{
    "name": "libcglm",
    "description": "C OpenGL Mathematics (GLM)",
    "version": [ 0,9,6 ],
    "git-url": "https://github.com/recp/cglm",
    "git-commit": "v0.9.6",
    "depends": [ "cross-gcc", "cmake-toolchain" ],
    "patches": [],
    "bootstrap-commands": [
        [ "cmake",
            "-S${repo_directory}/cglm", "-B.",
            "-DCGLM_USE_TEST=OFF", "-DCGLM_SHARED=OFF", "-DCGLM_STATIC=OFF",
            "-DCMAKE_INSTALL_PREFIX=${prefix}",
            "--toolchain=${bootstrap_directory}/toolchain.cmake",
            "-GNinja" ]
    ],
    "build-commands": [
        [ "cmake", "--build", "." ]
    ],
    "install-commands": [
        [ "cmake", "--install", "." ],
        [ "export", "DESTDIR=${bin_package_prefix}" ],
        [ "cmake", "--install", "." ],
        [ "cd", "${bin_package_prefix}" ],
        [ "mv", "./${prefix}/*", "${bin_package_prefix}" ],
        [ "rm", "-r", "`echo ${bin_package_prefix} | cut -d/ -f2`" ]
    ]
}
