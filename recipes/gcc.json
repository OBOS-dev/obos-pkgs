{
    "name": "gcc",
    "description": "Compiler used to compile",
    "version": [ 14,2,0 ],
    "depends": [ "cross-gcc>=14.2.0", "binutils>=2.35.0", "libgmp>=4.3.2", "libmpfr>=3.1.0", "libmpc>=1.0.1" ],
    "patches": [],
    "bootstrap-commands": [
        [ "${repo_directory}/gcc-14.2.0/configure",
                "--with-build-sysroot=${prefix}",
                "--target=${target_triplet}",
                "--prefix=${prefix}",
                "--with-sysroot=/",
                "--disable-nls", "--enable-languages=c,c++",
                "--disable-shared",
                "--disable-multilib",
                "--host=${target_triplet}",
                "--enable-threads=posix"
        ]
    ],
    "build-commands": [
        [ "make", "-j${nproc}", "all-gcc" ],
        [ "make", "-j${nproc}", "all-target-libgcc" ]
    ],
    "install-commands": [
        [ "make", "-j${nproc}", "install-gcc" ],
        [ "make", "-j${nproc}", "install-target-libgcc" ],
        [ "make", "-j${nproc}", "prefix=${bin_package_prefix}/usr", "install-gcc" ],
        [ "make", "-j${nproc}", "prefix=${bin_package_prefix}/usr", "install-target-libgcc" ]
    ],
    "inhibit-auto-rebuild": true
}
