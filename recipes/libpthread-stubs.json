{
    "name": "libpthread-stubs",
    "description": "Unknown",
    "version": [ 0,0,3 ],
    "url": "https://www.x.org/releases/X11R7.7/src/everything/libpthread-stubs-0.3.tar.bz2",
    "depends": [ "cross-gcc" ],
    "patches": [],
    "bootstrap-commands": [
        [ "${root_directory}/scripts/do-autoreconf.sh", "${repo_directory}/${name}-0.3" ],
        [ "${repo_directory}/${name}-0.3/configure", "--host=${target_triplet}", "--prefix=${prefix}" ]
    ],
    "build-commands": [
        [ "make" ]
    ],
    "install-commands": [
        [ "make", "install", "DESTDIR=${destdir}" ],
        [ "make", "install", "DESTDIR=${bin_package_prefix}" ]
    ]
}
