{
    "name": "distro-files",
    "description": "distro-files",
    "version": [ 0,0,0 ],
    "depends": [],
    "build-depends": [],
    "patches": [],
    "bootstrap-commands": [],
    "build-commands": [],
    "install-commands": [
        [ "${root_directory}/scripts/install-distro-files.sh", "${root_directory}", "${destdir}" ],
        [ "${root_directory}/scripts/install-distro-files.sh", "${root_directory}", "${bin_package_prefix}" ]
    ]
}
